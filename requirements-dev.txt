# Extra tooling for building the standalone app (not needed to run from source).
-r requirements.txt
pyinstaller>=6.0
pillow>=10.0  # only used to (re)generate aurantium.icns for the macOS build
# cryptography is in requirements.txt (needed at runtime by updater_mac.py);
# tools/sign_update.py and tools/gen_keys.py reuse that same install.
